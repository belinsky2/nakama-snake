# 🐍 Snake Game — Правила для AI-агентов

> Проект: Классическая игра "Змейка" с современным дизайном
> Стек: FastAPI (backend) + React (frontend) + SQLite (база данных)

---

## 📋 Описание проекта

**Snake Game** — браузерная игра "Змейка" с:
- Красивым современным дизайном и анимациями
- Таблицей лидеров
- Сохранением результатов в локальную БД
- Без авторизации (один пользователь)

---

## 🏗️ Структура проекта

```
snake/
├── backend/                 # Python FastAPI сервер
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py          # Точка входа FastAPI
│   │   ├── database.py      # SQLite подключение
│   │   ├── models.py        # SQLAlchemy модели
│   │   ├── schemas.py       # Pydantic схемы
│   │   ├── routers/
│   │   │   ├── __init__.py
│   │   │   └── scores.py    # API для результатов
│   │   └── settings.py      # Настройки
│   ├── requirements.txt
│   └── snake.db             # SQLite база (создаётся автоматически)
│
├── frontend/                # React приложение
│   ├── public/
│   ├── src/
│   │   ├── components/      # React компоненты
│   │   │   ├── Game/        # Игровое поле
│   │   │   ├── Menu/        # Главное меню
│   │   │   ├── Leaderboard/ # Таблица лидеров
│   │   │   └── UI/          # Кнопки, модалки
│   │   ├── hooks/           # React хуки (логика игры)
│   │   ├── api/             # Запросы к backend
│   │   ├── styles/          # CSS/стили
│   │   ├── App.jsx
│   │   └── main.jsx
│   ├── package.json
│   └── vite.config.js
│
├── docs/                    # Документация
├── .cursorrules             # Этот файл
├── .gitignore
├── .flake8
└── README.md
```

---

## 🎯 Технические требования

### Backend (Python)
- **Framework:** FastAPI 0.109+
- **Database:** SQLite через SQLAlchemy 2.0+
- **Validation:** Pydantic 2.5+
- **Python:** 3.10+

### Frontend (React)
- **Framework:** React 18+
- **Build:** Vite (быстрее чем Create React App)
- **Styling:** CSS Modules или styled-components
- **HTTP Client:** fetch или axios

### API Endpoints
```
GET  /api/health              # Проверка работы сервера
POST /api/scores              # Сохранить результат игры
GET  /api/leaderboard         # Топ-10 результатов
GET  /api/stats               # Статистика игрока
```

---

## 💻 Code Style

### Python (Backend)
- Type hints обязательны
- Docstrings для всех функций
- snake_case для переменных и функций
- PascalCase для классов
- Async/await для I/O операций

```python
async def save_score(score: int, player_name: str = "Player") -> ScoreResponse:
    """
    Сохранить результат игры в базу данных.
    
    Args:
        score: Количество очков
        player_name: Имя игрока (по умолчанию "Player")
    
    Returns:
        ScoreResponse с данными сохранённого результата
    """
    ...
```

### JavaScript/React (Frontend)
- Functional components с hooks
- camelCase для переменных и функций
- PascalCase для компонентов
- JSDoc для сложных функций

```jsx
/**
 * Игровое поле змейки
 * @param {Object} props
 * @param {Function} props.onGameOver - Callback при окончании игры
 */
const GameBoard = ({ onGameOver }) => {
  ...
};
```

---

## 🎮 Игровая механика

### Правила змейки
1. Змейка движется непрерывно в текущем направлении
2. Игрок управляет стрелками (↑↓←→) или WASD
3. При поедании еды: +1 к длине, +10 очков
4. Game Over при столкновении со стеной или своим хвостом
5. Скорость увеличивается каждые 50 очков

### Размеры
- Поле: 20x20 клеток
- Клетка: 20x20 пикселей
- Начальная длина змейки: 3

---

## 🎨 Дизайн

### Цветовая палитра
- Background: темный (#1a1a2e или подобный)
- Snake: градиент зелёный (#00ff87 → #60efff)
- Food: яркий акцент (#ff6b6b)
- UI: неоновые акценты

### Анимации
- Плавное движение змейки
- Пульсация еды
- Эффект при поедании
- Анимация Game Over
- Переходы между экранами

---

## ⚠️ Важные правила для агентов

1. **СНАЧАЛА читай документацию:**
   - `docs/project-plan.md`
   - `docs/technical-summary.md`
   - Этот файл `.cursorrules`

2. **Не усложняй:**
   - Нет авторизации (пока)
   - SQLite вместо PostgreSQL
   - Минимум зависимостей

3. **Пользователь — не разработчик:**
   - Объясняй все технические термины
   - Давай простые инструкции по запуску
   - Предлагай решения, а не спрашивай

4. **Локальный запуск:**
   - Всё должно работать на localhost
   - Backend: http://localhost:8000
   - Frontend: http://localhost:5173

5. **Changelog:**
   - После каждой волны — запись в `docs/changelogs/`

---

## 🚀 Команды запуска

### Backend
```bash
cd backend
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

### Frontend
```bash
cd frontend
npm install
npm run dev
```

---

## 📝 Definition of Done

- [ ] Игра запускается в браузере
- [ ] Управление работает (стрелки/WASD)
- [ ] Результаты сохраняются в БД
- [ ] Таблица лидеров показывает топ-10
- [ ] Красивый дизайн с анимациями
- [ ] README с инструкцией по запуску
