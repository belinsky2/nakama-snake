# 🔧 Техническое описание: Snake Game

> Этот документ — ИСТОЧНИК ПРАВДЫ о системе.

---

## 1. Обзор системы

**Назначение:** Браузерная игра "Змейка" с современным дизайном и сохранением результатов.

**Ключевые возможности:**
- Классический геймплей змейки
- Красивый неоновый дизайн с анимациями
- Таблица лидеров (топ-10)
- Локальное хранение результатов

---

## 2. Стек технологий

### Backend

| Компонент | Технология | Версия | Назначение |
|-----------|------------|--------|------------|
| **Язык** | Python | 3.10+ | Основной язык |
| **Framework** | FastAPI | 0.109+ | REST API |
| **ORM** | SQLAlchemy | 2.0+ | Работа с БД |
| **База данных** | SQLite | — | Хранение результатов |
| **Валидация** | Pydantic | 2.5+ | Схемы данных |
| **Сервер** | Uvicorn | 0.27+ | ASGI сервер |

### Frontend

| Компонент | Технология | Версия | Назначение |
|-----------|------------|--------|------------|
| **Библиотека** | React | 18+ | UI компоненты |
| **Сборщик** | Vite | 5+ | Быстрая сборка |
| **Язык** | JavaScript/JSX | ES2022 | Логика и разметка |
| **Стили** | CSS Modules | — | Изолированные стили |

---

## 3. Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                         БРАУЗЕР                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    React Frontend                          │  │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────────┐  │  │
│  │  │  Menu   │  │  Game   │  │GameOver │  │ Leaderboard │  │  │
│  │  └─────────┘  └─────────┘  └─────────┘  └─────────────┘  │  │
│  │                      │                          ▲          │  │
│  │                      ▼                          │          │  │
│  │              ┌──────────────┐                   │          │  │
│  │              │ useSnakeGame │ ──────────────────┘          │  │
│  │              │    (hook)    │                              │  │
│  │              └──────────────┘                              │  │
│  └───────────────────────────────────────────────────────────┘  │
│                              │ HTTP                              │
└──────────────────────────────┼───────────────────────────────────┘
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FastAPI Backend                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │
│  │   Routers   │  │   Models    │  │      Database           │  │
│  │  /scores    │──│   Score     │──│   SQLite (snake.db)     │  │
│  │  /health    │  │             │  │                         │  │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. Компоненты системы

### 4.1 Backend (`backend/app/`)

#### `main.py` — Точка входа
```python
# Создание FastAPI приложения
# Подключение роутеров
# Настройка CORS
# Создание таблиц БД при старте
```

#### `database.py` — Подключение к SQLite
```python
# SQLAlchemy engine и session
# DATABASE_URL = "sqlite:///./snake.db"
```

#### `models.py` — SQLAlchemy модели
```python
class Score:
    id: int              # Уникальный ID
    player_name: str     # Имя игрока (default: "Player")
    score: int           # Количество очков
    snake_length: int    # Длина змейки
    duration_seconds: int # Длительность игры
    created_at: datetime # Когда сыграно
```

#### `schemas.py` — Pydantic схемы
```python
class ScoreCreate:       # Для создания записи
class ScoreResponse:     # Для ответа API
class LeaderboardEntry:  # Для таблицы лидеров
class StatsResponse:     # Для статистики
```

#### `routers/scores.py` — API endpoints
```python
POST /api/scores         # Сохранить результат
GET  /api/leaderboard    # Топ-10
GET  /api/stats          # Статистика игрока
GET  /api/health         # Health check
```

### 4.2 Frontend (`frontend/src/`)

#### `hooks/useSnakeGame.js` — Игровая логика
```javascript
// Состояние игры: snake, food, direction, score, gameOver
// Функции: moveSnake, changeDirection, resetGame
// Управление: обработка клавиш
// Таймер: setInterval для движения
```

#### `components/Game/GameBoard.jsx` — Игровое поле
```javascript
// Canvas для отрисовки
// Рендер змейки и еды
// Анимации
```

#### `components/Menu/Menu.jsx` — Главное меню
```javascript
// Кнопка "Начать игру"
// Кнопка "Таблица лидеров"
```

#### `components/Leaderboard/Leaderboard.jsx` — Таблица лидеров
```javascript
// Загрузка данных с backend
// Отображение топ-10
```

#### `api/scores.js` — HTTP клиент
```javascript
// saveScore(score, snakeLength, duration)
// getLeaderboard()
// getStats()
```

---

## 5. API Reference

### POST /api/scores

**Описание:** Сохранить результат игры

**Request:**
```json
{
  "score": 150,
  "snake_length": 18,
  "duration_seconds": 120
}
```

**Response (201):**
```json
{
  "id": 1,
  "player_name": "Player",
  "score": 150,
  "snake_length": 18,
  "duration_seconds": 120,
  "created_at": "2026-01-15T12:00:00"
}
```

### GET /api/leaderboard

**Описание:** Получить топ-10 результатов

**Response (200):**
```json
{
  "entries": [
    {"rank": 1, "player_name": "Player", "score": 500, "created_at": "..."},
    {"rank": 2, "player_name": "Player", "score": 350, "created_at": "..."}
  ]
}
```

### GET /api/stats

**Описание:** Получить статистику игрока

**Response (200):**
```json
{
  "total_games": 25,
  "best_score": 500,
  "average_score": 180,
  "total_play_time_seconds": 3600
}
```

---

## 6. Игровая механика

### Параметры

| Параметр | Значение | Описание |
|----------|----------|----------|
| Размер поля | 20×20 | Клетки |
| Размер клетки | 20×20 px | Пиксели |
| Начальная длина | 3 | Сегменты змейки |
| Начальная скорость | 150 мс | Интервал движения |
| Очки за еду | 10 | За каждую еду |
| Ускорение | -5 мс каждые 50 очков | Увеличение сложности |
| Мин. скорость | 50 мс | Предел ускорения |

### Управление

| Клавиши | Действие |
|---------|----------|
| ↑ или W | Вверх |
| ↓ или S | Вниз |
| ← или A | Влево |
| → или D | Вправо |
| Space | Пауза |
| Enter | Начать/Перезапустить |

### Game Over

Игра заканчивается при:
1. Столкновении с границей поля
2. Столкновении с собственным хвостом

---

## 7. Запуск

### Backend

```bash
cd backend
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

**Проверка:** http://localhost:8000/docs

### Frontend

```bash
cd frontend
npm install
npm run dev
```

**Игра:** http://localhost:5173

---

## 8. Структура файлов

```
snake/
├── backend/
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py           # FastAPI app
│   │   ├── database.py       # SQLite connection
│   │   ├── models.py         # SQLAlchemy models
│   │   ├── schemas.py        # Pydantic schemas
│   │   └── routers/
│   │       ├── __init__.py
│   │       └── scores.py     # API endpoints
│   ├── requirements.txt
│   └── snake.db              # SQLite database (auto-created)
│
├── frontend/
│   ├── public/
│   │   └── index.html
│   ├── src/
│   │   ├── components/
│   │   │   ├── Game/
│   │   │   │   ├── GameBoard.jsx
│   │   │   │   └── GameBoard.module.css
│   │   │   ├── Menu/
│   │   │   ├── Leaderboard/
│   │   │   └── UI/
│   │   ├── hooks/
│   │   │   └── useSnakeGame.js
│   │   ├── api/
│   │   │   └── scores.js
│   │   ├── styles/
│   │   │   └── global.css
│   │   ├── App.jsx
│   │   └── main.jsx
│   ├── package.json
│   └── vite.config.js
│
├── docs/
├── .cursorrules
├── .gitignore
├── .flake8
└── README.md
```
